import { Routes } from '@angular/router';
import { InvoiceShellComponent } from './Features/Invoices/invoice-shell/invoice-shell.component';

export const routes: Routes = [
  { path: '', component: InvoiceShellComponent },
];